use crate::models::course::Course;
use crate::models::student::Student;

pub fn validate_student(student: &Student) -> bool {
    // 在此添加对学生信息的验证逻辑
    true
}

pub fn validate_course(course: &Course) -> bool {
    // 在此添加对课程信息的验证逻辑
    true
}
